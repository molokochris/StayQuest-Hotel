import React from "react";

export default function Book({ navigation }) {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Book
    </div>
  );
}
